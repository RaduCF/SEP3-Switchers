@page "/Products"

@using RestSharp
@using Newtonsoft.Json
@using WebApplication.Data
@inject WebApplication.Data.Connection connection
@inject WebApplication.Data.Wishlist internalWishlist

<style>
    #titlediv {
        margin-top: 5%;
        margin-left: 5%;
    }

    #itemdiv {
        margin-top: 1.5%;
        height: 150px;
        width: 90%;
        border: 1px solid #ccc;
        border-radius: 4px;
    }

    #imagediv {
        float: left;
        width: 23%;
        height: 148px;
        border: 1px solid #ccc;
        border-radius: 2px;
    }

    #photo {
        width: 100%;
        height: 100%;
    }

    #itemDescDiv {
        float: right;
        width: 70.9%;
        height: 100%;
        border: 1px solid #ccc;
        border-radius: 2px;
    }

    #adddiv {
        width: 6%;
    }
</style>

<div id="titlediv">
    <h2 class="display-4">Your search results:</h2>
</div>

<ul class="list-unstyled">
    @foreach (var item in connection.Final)
    {
        <li id="@index">
            <div id="itemdiv">
                <a href="@item.URL" target="_blank">
                    <div id="imagediv"><img src="~/Images/no-img.png" id="photo" alt="Product image." /></div>
                    <div id="itemDescDiv">
                        <p>@item.Title</p>
                        <br />
                        <p>Rating of the product: @(Math.Round(item.Rating)</p>
                        <br />
                        <p>Price: @item.Price @item.PriceCurrency</p>
                    </div>
                </a>
                <div id="adddiv">
                    <button id="add" class="btn btn-danger" @onclick="AddToWishList">Add to wishlist</button>
                </div>
            </div>
        </li>
        <li hidden>@(index++)</li>
    }
</ul>

@code{
    public int index { get; set; } = 0;

    private void AddToWishList()
    {

        UserWish userWish = new UserWish { ID = internalWishlist.Username, Title = connection.Final.ElementAt(index).Title, URL = connection.Final.ElementAt(index).URL };
        internalWishlist.AddWish(userWish);
    }
}